 trigger: none

 pool: Keda

 variables:
   buildConfiguration: 'Release'
   
 jobs:
 - job: job1
   steps:
   - task: Bash@3
     inputs:
       targetType: 'inline'
       script: ''
     displayName: Check if job is running